# Generative AI multi agent fine tuning Overview

**Date:** December 08, 2025
**Source:** otter.ai

<audio controls>
  <source src="./Generative AI multi agent fine tuning Overview.mp3" type="audio/mpeg">
  Your browser does not support the audio element. <a href="./Generative AI multi agent fine tuning Overview.mp3">Download audio</a>
</audio>

## AI Summary
### Overview

The team from AWS's Generative AI Innovation Center discussed their approach to fine-tuning language models, focusing on balancing accuracy, cost, and latency. They highlighted the shift towards smaller, cheaper models under 7 billion parameters, which offer cost savings but have limitations in generalization. Their solution involves a multi-agent system that automates data augmentation and fine-tuning, using pattern analysis and quality agents to improve model performance. Experiments showed an average 29% improvement over baseline fine-tuning, with the system achieving significant cost and time savings. The team emphasized the importance of automated processes in scaling model customization.

Action Items

More options

Complete the quick session survey in the app before leaving.

Share the paper link / QR code, program links, and the FAQ/resources referenced during the talk with attendees.

Provide access to the agent workflow artifacts (pattern summaries, generation strategies, and quality-agent feedback) so model developers can review and edit generation instructions.

Assess the need and design for a human-in-the-loop review step for cluster outcomes (e.g., when to surface clusters for manual inspection) and report recommended changes.

Investigate and prototype additional agents (data-cleaning agent, hyperparameter-suggestion agent) and a dynamic orchestrator that decides which agents to run.

Continue running large-scale automated fine-tuning experiments using the pipeline (schedule next batch and monitor resource needs).

### Outline


**Introduction to the Team and Mission**

- Speaker 1 introduces the team, mentioning their roles at the AWS Generative AI Innovation Center.
- The team's mission is to help customers design, prototype, and launch generative AI solutions at scale.
- The Innovation Center pairs science and strategy experts with customers to accelerate generative AI adoption on AWS.
- The team works hands-on with customers to move AI solutions into production.

**Challenges with Fine-Tuning Language Models**

- Speaker 1 discusses the challenges of fine-tuning language models, focusing on accuracy, cost, and latency.
- Larger models achieve higher performance but are expensive to train and run at scale.
- Latency is crucial for end-user experience, and small models are preferred for their efficiency.
- Customers often make trade-offs between accuracy and cost in production environments.

**Shift Towards Smaller Language Models**

- Speaker 1 explains the shift towards smaller, faster, and cheaper language models for production.
- Small models require less GPU memory and compute, making them cost-effective.
- They generate tokens faster, reducing latency and improving customer experience.
- Training small models is challenging due to their inherent limitations in generalization and reasoning capabilities.

**Customization Techniques for Language Models**

- Speaker 1 outlines various customization techniques, including supervised tuning, reinforcement learning, and pre-training.
- The team recommends starting with simple techniques like prompt engineering before moving to more complex methods.
- Supervised fine-tuning is highlighted as an effective method for learning domain-specific patterns and terminology.
- The upfront cost of customization includes investments in duration, augmentation science, engineering efforts, and infrastructure costs.

**Multi-Agent System for Automating Fine-Tuning**

- Speaker 1 introduces the multi-agent system designed to automate the iterative refinement process for small language models.
- The system addresses the challenges of data preparation, model fine-tuning, and error analysis.
- It orchestrates the analysis of training errors and generalization gaps to improve model performance.
- The automated end-to-end training pipeline validates data augmentation strategies quickly and efficiently.

**Design and Functionality of Multi-Agent System**

- Speaker 3 provides a high-level overview of the multi-agent system, including the orchestrator, data generation agent, and quality agent.
- The orchestrator executes the workflow, while the data generation agent generates additional training data based on error patterns.
- The quality agent ensures the quality of generated samples to avoid hallucination and other issues.
- The system automates the entire fine-tuning pipeline, from configuration to model evaluation.

**Detailed Explanation of Agents and Their Roles**

- Speaker 3 explains the design and functionality of each agent in the multi-agent system.
- The pattern analysis agent identifies error patterns and generates targeted data strategies.
- The quality agent ensures high-quality samples by providing scores and feedback.
- The system uses clustering techniques to reduce the number of invocations and improve operational efficiency.

**Experimentation and Results**

- Speaker 3 presents the experimentation setup and results, comparing the multi-agent approach with baseline fine-tuning.
- The multi-agent approach achieves an average of 29% relative improvement over the baseline.
- Qualitative results from various data sets highlight the effectiveness of the system in addressing specific error patterns.
- The system's design allows for dynamic orchestration with additional agents for fine-tuning.

**Conclusion and Future Work**

- Speaker 3 concludes by summarizing the key findings and future work for the multi-agent system.
- The system addresses the shift towards using small language models and improves their performance through automation.
- Future work includes expanding the system with additional agents and dynamic orchestration.
- The team plans to present their research at the upcoming conference.

**Q&A Session**

- Speaker 1 opens the floor for questions, addressing concerns about data leakage, model compatibility, and automation.
- Speaker 3 explains how the system prevents data leakage by using the training data for augmentation.
- The system has been tested with multiple models, with varying results based on model size and complexity.
- The automated pipeline requires minimal manual intervention, making it highly scalable and efficient.

## Speakers

- Speaker 1
- Speaker 2
- Speaker 3
- Speaker 4
- Unknown Speaker

## Transcript

**Unknown Speaker (00:00):**  
You first, which enabled us to actually run

**Unknown Speaker (00:11):**  
hundreds of experiments within just

**Speaker 1 (00:16):**  
a few weeks. And then we'll also dive a little bit deeper into the individual agents in our multi agent architecture and talk about the different experiments that we ran in order to improve that methodology. And finally, we'll discuss our future works and conclusions. Okay, so let me give you a quick introduction to our team. So Juan and I are part of the AWS generative AI Innovation Center. Our core mission is to help customers design useful Gen AI solutions, quickly build and prototype them, but then also launch them in production at scale. The Innovation Center, we pair our science and strategy experts with customers to accelerate the adoption of generative AI on AWS. So we work hands on keyboard and customer environments to help over time WAF solutions and then move them into production. Our team is a substitute within the innovation center that specializes in model in performance that are

**Speaker 2 (01:22):**  
available. What

**Speaker 1 (01:29):**  
customization technique and then also make those objectives a reality. So this will include things like supervised on tuning for domain adaptation, reinforcement learning and continued pre training, and also training and inference optimizations. All right, so let's get into the meat of our session. And first up, we'll talk about challenges with fine tuning language models. So in customizing models, what you're really looking to do is find the sweet spot between accuracy, cost and latency. These three pillars, we need models that deliver correct, viable outputs for specialized domains. But as performance improves, we often face diminishing returns, sometimes pushing for that last 5% of improvement. It does come at a steep price, cost encompass both training and inference. So larger models with more parameters, they generally achieve higher performance out of the box, but they are expensive to train and they are costly to run at scale. So for enterprise applications serving millions of requests, inference costs can quickly accumulate. Latency for most customers is where the rubber meets the road. For our end user experience. You might have a large model with very high performance, but if it takes 30 seconds to respond for an online application, that's that's just not going to work. And so the tension between these three pillars, it's actually quite natural, and how you choose to make these trade offs, it ultimately comes down to your business priorities and what's most important to you. We often find that customers, in their initial experimentation phases, they will really strive for high accuracy metrics, but then when they deploy the production, they're actually okay with making trade offs, perhaps going with a slightly less performant model that's faster and less expensive, right? So a bit of a trade off between the three. And this is actually why we're seeing customers make a fundamental shift in how they think generative AI models in production. The initial philosophy around models was one large one that does everything, but because these foundation models, they prove too expensive to be put into production at scale and too slow for real time applications. So that's driving the industry towards smaller, faster and cheaper language models that are specialized for smaller scopes. Small language models are generally considered under 7 billion parameters. They require a fraction of the GPU memory to train and less compute to host. You can deploy them on cost effective infrastructure, removing the need for the latest and most expensive hardware, small language models, they generate tokens faster and more efficiently, which reduces latency and enables online use cases and also improves your customer experience. They also require less compute to trade. But the more important win is on inference costs. Reduced token consumption means lower cost per request, and when you're serving millions of queries per day, those savings will compound quickly. So that said training small language models is not without its challenges. Small language models, they are inherently less capable. Models under 7 billion parameters have size constraints that fundamentally impact their ability to generalize. They don't have the same knowledge breadth or reasoning capabilities as larger foundation models do small language models. They also struggle when there are significant gaps in training data. Traditional techniques to address these gaps, they typically involve up sampling the data with semantically similar synthetic samples. You can show examples of your training data to an LLM and say, Hey, generate more samples that look like this. But approaches like this, they fall short when it comes to helping the model generalize. As the model will only generate samples within the same distribution as what was provided. So when we observe gaps and the performance of the foundation model. The next step is to consider some kind of customization technique. There are many ways to customize model that vary in terms of cost and complexity. This is just a sub sample of some of the techniques that our team covers. Your our general recommendation is to start simple and identify what gaps you're trying to close before you consider customization techniques like supervised tuning or reinforcement learning, you'll want to make sure that you have extracted the most out of similar techniques like prompt engineering that leverage the existing capabilities of a foundation model or retrieval augmented generation rag that augment foundation model capabilities With your proprietary data. And when these techniques fall short, consider ones that actually make modifications to the model weights. Leveraging these techniques increases the complexity, but we generally see that they lead to more significant gains in terms of model quality and the agent's ability to accomplish a desired outcome. So today, we're going to focus primarily on supervised fine tuning, which is a good starting point for any type of model customization, and it's effective when you want the model to learn domain specific patterns, terminology and structured output formats. And I would be remiss in talking about model customization without at least mentioning that there is an upfront cost and a semi reoccurring cost to do so, even for fine tuning, which does strike a good balance between the implementation complexity and resource consumption, the cost includes investments into duration and augmentation, science and engineering efforts and infrastructure costs, And then, of course, the periodic updates to the model. But that said, we see generally that spending a little upfront to optimize model, it does lead to long term cost advantages through optimize soaking consumption and reduced inference expenses, customized agents say, make fewer mistakes and perform more efficiently against your business use case than off the shelf models do. You can fine tune a smaller model to perform as well as a larger one on your target use case, which will give you the benefits of reduced latency and cost. So here we're left with a little bit of a conundrum. So we would like to use smaller language models for production economics, but they require more effort to train, and they perform worse out of the box. And this is exactly the problem that our multi agent system was designed to solve, automating the iterative refinement process and systematically addressing gaps in data to unlock the potential of small language models. Okay? So the motivation for automating our pipeline and our multi agent design, it comes from our first net experience with customer engagements. We work with customers every day to customize models, and have observed a consistent pattern in the fine tuning procedure. First, of course, you start with data preparation and augmentation. And as many of you in the audience know, this is often the long pole in the tent. Data needs to be representative of our business use case, and data needs to be augmented and massaged before it can be used for training. Once the data is ready, we move into the finding model fine tuning phase, which involves selecting a model customized there's, of course, infrastructure set up, and then performing the hyper parameter optimization for the training process. And then when the model has been fine tuned, we make inference on a validation data set, and then we do error analysis to check the model's responses. And the process then repeats. The insights from animation worked, the insights from error analysis that get done into the next round of data preparation, where we focus on addressing the weaknesses by augmenting the data for the next round of trading. So for example, if the model isn't good at handling complex queries for a customer support use case, then we of course, have to prepare more data that looks like that for that scenario, and then we improve the model in the next round of training. So this can repeat for several iterations until the model performance is up to par for whatever use case we're targeting. And naturally, we ask ourselves, is a repetitive process? Can we automate it so we can reduce the manual human effort and make development faster, more streamlined and more scalable? So this is the main idea for our solution, but it's challenging, augmentation, augmenting the original training data. It might seem like a simple task. We can ask the L button to generate more data that looks like what we have, but the main challenge actually comes from generating training data that actually improves the model's performance. This is challenging because the search space is huge, and there are so many different ways that you can generate training data, most of which the techniques will not work. And so each time we try a new one, it costs us in terms of both time and resources. So what we really want is a better way of identifying data strategies that have the highest chance of improving model performance, and then a way to quickly validate the performance in data groups. So to do this, we need to train the model with augmented data and then perform the validation error analysis, and then it's the training process. It requires a lot of manual time and effort, so we need an automated, scalable trial process to do the actual training and see how the augmentation strategies perform. So it. So our solution addresses these two challenges with fine tuning language models. We developed a multi agent, multi agent system that orchestrates the analysis of training errors and generalization gaps in previous model training rounds. It directly targets the data generation problem. We focus on analysis and investigation of errors made by the model to have more targeted strategies that have a higher chance of improving performance

**Speaker 1 (11:08):**  
for mistakes the model has made. We have more insights into how to improve the model in the next round, in terms of both the training errors and the generalization gaps to unseen validation data, the second component is an automated end to end training pipeline, which enabled us to quickly validate data augmentation strategies. We automated the whole pipeline from training, deployment, inference and evaluation in sagemaker Ai, okay, so we're gonna do a couple of quick primers here. So first, we'll start with a primer on agents. So at AWS, we define an AI agent has an autonomous software system that leverages AI to reason, plan and complete tasks on behalf of humans or systems. I actually, I come from a software engineering background, so I like to think of them as microservices. If that's a helpful frame of reference for you guys, an agent is a system built on top of an LLM that has agency. Agents enable llms to take action by themselves. They're just models that operate with inputs and outputs. So agents enable llms to take action and have higher levels of automation that take advantage of the llms Advanced reasoning capabilities. They don't just answer questions. They solve problems. Interact with tools. They call APIs, they retrieve information, and they make decisions based on context that has been provided. They maintain state across multiple interactions, and can adapt when things do not go as planned. So there are several architectures to consider when building an objective system, you can go with a simple tool based approach with predefined workflows and business logic. But as we all know by now, these workflows are rigid. They're not very flexible, and they're hard to adapt as business use cases at all. You can go with a single agent. These are capable. Can perform well on simple use cases, but as you start providing the agents with more tools and more complex challenges, this can lead to confusion and performance bottlenecks. Multi agent designs, they address the shortcomings of a single agent in that they're more scalable and modular individual agents. They can specialize in specific tasks or tool sets, and you can optimize and scale these agents individually, so that has some deployment benefits too. And then finally, you can go with the hybrid approach. For example, having a simple rules based orchestrator and then specialized sub agents for individual tasks, which is actually what our solution ended up looking like in the end. So multi agent architectures in their simplest form, they have two primary components. You have your orchestrator and various sub agents. So orchestrators, they have a global scope. They take in a query from the user and break them down into individual sub tasks. The orchestrator is aware of the status of all sub agents and can interact with them freely to manage a given plan, but they cannot execute tasks on their own. They need to do so by asking an agent. The sub agent is more focused on a single sub task and only aware of its own context. It can interact freely with its knowledge base and can call tools to achieve its goals, but when it's done, it reports its results back to the orchestrator for response synthesis, and real quick, a primer on supervised fine tuning, since we've mentioned it a few times, and want to make sure that we're level setting so supervised fine tuning, it's a method of training, a base foundation model on a task specific label data set to specialize for a given use case. Models often encounter domain specific scenarios that they don't handle well out of the box. Take a medical assistant, for example, it needs to understand specialized medical terminology, how to follow specific regulatory formats and also produce structured outputs that downstream agents can reliably parse a general purpose foundation model. It might get close, but it's unlikely to consistently handle the nuances of your domain and perform well on these specialized cases that have not been seen in its pre training data. With supervised fine tuning, you train models with inputs and output pairs showing the desired behavior for your use case. And for these examples, the model learns how to handle your domains, patterns, terminology and also, before we move on, let me at least talk at a high level about some of your different approaches to fine tuning an agent. So broadly speaking, we have two approaches. You may have heard some of these terms before. We have full fine tuning, which updates all the model parameters, and this gives you maximum customizability, but also comes at a higher computational cost. And we also have parameter efficient methods. This is the acronym is pet, which updates a small fraction of the parameters through techniques like low rank adaption. Laura parameter. Efficient methods are faster and less expensive, and you can train multiple task specific adapters that share a baseline model. This lets you customize multiple specialized agents without multiplying your infrastructure costs. These there are use cases for preventing but we generally find that parameter efficient methods are preferred amongst our customers for their cost efficiency and also their deployment flexibility. We recommend trying these parameter efficient methods first, before you try methods that are more involved. Okay, I'm gonna pass you off to Juan now he's gonna dive a little bit deeper into the solution that you find.

**Speaker 3 (16:28):**  
All right. Thank you, Chauncey, Nana, now I want to give a high level overview of the two critical components in our solution. The first one is multi agent, data augmentation, and the second one is end to end, fine tuning pipeline automation. Let's start with the multi agent data augmentation. First at the top, we have the orchestrator, which sees the entire agentic workflow, and IT has access to all the context information. And importantly, we also have access to this model, which model which has been initially fine tuned with the original training data. Now remember, our task right now is to go from the original training data to the augmented data set, which can hopefully improve the model's training performance. And that's exactly the design of our data generation agent. It takes the additional the original data to generate additional training data. And as Charlene has mentioned, the data generation is actually pretty challenging, because there are so many degrees of freedom, and we need to find out the most effective data samples to generate. That's why we designed two critical components in the agentic workflow as the upstream of data generation agents. We design a pattern analysis agent. Its responsibility is to provide recommendation as well as guidance about how we generate the most effective samples. And our philosophy is to collect the error samples and conduct error analysis based on the errors to generate the most targeted data strategies. And as a downstream of the data generation agent, we also design a quality agent. Its role is to ensure the quality of the generated samples, as Yan probably made. Familiar with, the model fine tuning process is highly sensitive to the generated sample qualities, so want to make sure only the high quality samples got passed through the quality agent and was actually used for model fine tuning and for the samples that didn't pass the checks from the quality agent. The quality agent will also provide the feedback for the generation agent, for it to rework, to regenerate, hopefully higher quality samples. So as I mentioned, the high quality samples will be fed into our pool of augmented training data. And will be this training data will be used for the next round of language model fine tuning. And similarly, as a model developer usually iterate between the process of fine tuning the model and preparing higher quality data, this workflow can also repeat itself for multiple rounds until there's no further model improvement. So with the generated training data, we also want to quickly test out whether it works by doing the actual fine tuning to see whether this performance improvement or any performance degradation. That's why a quick testing on the actual fine tuning is pretty critical, and that's the motivation for us to automate entire fine tuning pipeline, starting from the input as the configuration of the end to end training job, the pipeline automatically set up the trainer with a hugging face s on SDK, it also automatically trigger the sagemaker training job and also the sagemaker endpoint, and all the way to the model inference as well as custom evaluations. So this is actual command to run the entire pipeline. This is a single command, and once this has been executed, the entire pipeline will run from the beginning to the end, all by itself, without any human intervention. And the key component to enable this end to end, ROM is a status checking controller. It keeps tracking the status of each of the step pipeline, and once it sees that step has been completed, it automatically triggers the next step in the pipeline. And here I'm showing some real logs from our end to end job. You can see as a certain time span the training job has been started and is waiting for the capacity to start, and after about 20 minutes, the training job has been needed successfully. And this is checker automatically the next step, which is the deployment model to a sagemaker endpoint. And after a few minutes, they see that the endpoint has been serviced, so it automatically triggers the inference as well as evaluation, and in the end, the evaluation results, as well as all the intermediate artifacts, will be saved to actually bucket For later inspection. So this fine tuning automation is pretty impactful from the feedback of our model development team. This automation saves them about 30% of development time in fine tuning every single model, and the time saving comes from the fact that previously they need to manually monitor the status of the training job, and after the model has been trained, to manually copy paste the model ID, to deploy the model and so on. Now this whole process has been automated. There's no intervention needed in the process. And besides time saving, it also enables us to scale out to large scales. Because there's no human intervention needed, the developer can just trigger a large number of training jobs in the batch so they run in parallel as long as the compute resource permits. In the recent project of eight weeks of model development, the team was able to complete 700 end to end jobs, and this of this scale of experimentation was unthinkable before we were able to automate the entire

**Speaker 3 (22:59):**  
background. Now I hope the overall idea of our design for the multi agent data augmentation as well as the fine tuning Python automation, are clear. In the next section, I'm going to focus on just multi agent data augmentation components and deep dive specific design of how we design each of the agents and make sure they work well in conjunction. So starting with this high level overview, we have the orchestrator on the top, and it executes the sequential operation from the patent analysis agent to the actual data generation agent, and with the quality agent in the chats to ensure the quality of generated sample, but within every agent, we made very specific design to make sure they execute exactly as we expect. And all the agents together, they function smoothly when they are put into this sequential workflow. Now I'm going to go through the specific design for every single agent one by one. So starting with the pattern analysis agent. Our initial idea was pretty simple. Now we're trying to teach the model certain skill with fine tuning by providing examples to it. And the simple way to check how we improve the model training is to check which samples the model hasn't captured well during the training process. So basically it means that we can simply collect the incorrect training samples by the model and provided to generation agent. And we do a little bit of role playing from engineering into the generation agent. We're basically saying, assume now you are a teacher and helps the model to learn some students, which is a model to learn certain skills. And here are some examples. The student hasn't learned well, and your task is generate similar examples to the students as learning materials so they can learn better. So this is a simple and also very powerful idea, but in reality, it quickly runs into some robots, and the issue specifically comes with model over feeding. So this is the real performance we observed in our experiments. The training set performance almost separates, but at the same time, the validation set performance is much lower. So with such a high accuracy on the training set, it means that there are very few errors made by the model. The model basically memorize most of the training samples, so there are few errors for us to analyze and also to inform how we generate additional training data. And our main idea here is to instead leverage the validation set to focus on the generalization gap from the training data to the unseen validation set, and hopefully to use the validation gap to inform how we generate additional training data so that we can close this gap. So this is our main design for the passive analysis agent. We add the generalization analysis with very incremental steps to gradually build out the data generation strategies. Now note that we cannot directly fit the validation data to the model training process, since they need to be remain unseen as a holdout data set. And our idea here is to ask the patent analysis agent to come up with some analysis of the root cause as well as the error scenarios the root cause indicate what could be the potential cost for the model's error on this specific validation sample, and the scenario indicates what kind of situations model made those specific errors. From the sample level analysis, we need a round of patent categorization. We're trying to group those sample sample level analysis into tight clusters here using a clustering technique based on the text embeddings. And the next step is just to ask the pattern analysis agent to summarize each cluster. And those summarizations of each cluster represent the patterns, the error patterns we're trying to tackle. Here's a real example from our experimentation, and the task is question answering for certain scientific concepts and all of these clusters, two clusters stood out to us. One is that the patent analysis identified the model capability gap understanding biological and ecological concepts, and the other cluster is a gap in understanding physics as well as mathematics concepts. Now how do we go from the identified patterns, error patterns, to the actual generation strategies. We further define strategy drafting staff based on the identified patterns. We ask the patent analysis agent to come up with a few sentences of ideas about how we can generate the additional data to address the gap that has been identified. So here are the real generation strategies we obtained from the agent based on the previously identified patterns to address the gap on biological and ecological capability. The model suggests we generate samples related to evolutionary factors, species relationships and so on, and to tackle the gap of the mathematics and physics, the agent suggests that we generate samples related to simulated experimentations. So Yan, now here's our major learnings when we develop these Pattern Analysis agents, the first one is that we want to spend more tokens to that model. Think we are building the incremental steps we have actually tried out developing the patent analysis agent by removing certain steps, for example, remove the patent regularization, or remove the strategy drafting, but they didn't work well as of the whole pipeline. And the second learning is that we need to be aware of the limitation of agents we have tried out to directly fit all of the sample level error analysis to LLM for it to come up with certain characterization of the error patterns, but it didn't work well, and the reason is because there are too much information to the LLM to capture if there are certain small and distinct patterns within the large data set, the LLM is not able to capture that. So instead, we provide existing method using the clustering to have the characterization beforehand, and in the next step, the LM just needs to summarize each of the distinct cluster so the other eight cloud agent we developed is the quality agent, and this role is to mitigate the generation issues like hallucination, to make sure all these high quality samples will be used in the next round of model client reading. And we use a scoring method from the quality agent. For every sample generated, the agent will provide a score, and only the samples passing of greater than the score will be used. And for samples didn't meet the threshold, the quality agent will provide feedback for the generation agent to rework and in our development, we find out that it's important to use a different model between the generation agent and the quality agent. If you would, find out that you use the same algorithm to generate and to evaluate its own generation. Imagine that everything generates is pretty good. So we call it the self enforcing bias. So we want to avoid this bias as much as possible. And we find out it's pretty effective to just use a different as an evaluator, as opposed to the generator. So there's a real example from a code completion data set in our experiments, the original sample can contain the code snippet of a mean absolute deviation calculation as well as the expected response for model training and the data generation agent actually learns to generate a variant of this original problem on meaning absolute deviation. However, in the generated response provided by the agent, the return statement is really complex, super long, and there could be some errors within it, so we want to make sure these lower quality samples are not going to be used for model fine tuning.

**Speaker 3 (32:44):**  
And luckily, this sample was captured by our quality agent. Besides indicating that the sample is not good enough for fine tuning, they also provide the feedback that you should reduce the code complexity immediate calculation, and also you should simplify return statement to improve readability. And by incorporating this feedback in the next round of generation in the generation agent, it was able to return a higher quality sample with a much streamlined return statement. And this sample passes the checks for our quality agent. So let's conclude the walkthrough of different design for each agent. Now if you think about the entire workflow, it involves multiple element calls, and how do we ensure the operational efficiency? We utilize the three different approaches. The first one is action. As you already see, we have we provide the samples to the LM one by one for M analysis, so that will be pretty expensive, involving a lot of l1 invocations. And instead of that, we provide multiple samples in a single l1 invocation, and we ask the l1 to analyze or score with multiple samples in a single column. So this immediately cuts down invocation count, for example, from five to one, and the second approach is sub sampling. We have, we have to do some clustering to identify the patterns, and we don't have to use all of the samples, as long as the representative samples they are within the cluster, the LF will be able to capture those in its own summarization. So before clustering, we down sampled the validation errors to 50. And during the summarization, by looking at each cluster, basically the contact window will be cut down by half for the L column.

**Unknown Speaker (34:58):**  
And the third approach is a cluster based analysis,

**Speaker 3 (35:03):**  
although initially we did sample level analysis, but after the clustering has been done, all of the LME locations on identifying the error patterns as well as the generation strategies, they are per cluster. So the number of invocations, they will be equal to the number of clusters, which is typically very small,

**Speaker 3 (35:29):**  
so we have done extensive experiments to validate whether this solution is effective. And here is our experimentation setup. On the top, we show the models we use for the entire workflow. We're trying to fine tune the lambda 3.1 1b the small language model and the agent for it to for it to execute effectively. We need to utilize a much bigger model to leverage the high capability. And we utilize number 3.37, TB model. The only exception is the quality agent. We find out that it needs to be a different l1, and then the quality agent to avoid any bias. So we use, and we experimented with multiple data sets to validate the impact failures. And this data set cover various domains, such as factor, QA, table based POA, common sense reasoning, mass reasoning, as well as coding for each data set, we sample 1000 training examples in all of the fine tuning experiments. So here's our results for all of the data sets shown we compare with the baseline fine tuning. This is the Manila approach, without any data organization, just using the original training data. And our approach is on the second row, a multi agent orchestration. And as you can see, for every data set, we're able to achieve improvement over the baseline fine tuning. And on average, we achieved 29% relative improvement over the baseline fine tuning approach. Now I have to share some qualitative results from some interesting clusters we identified in the experimentation data set. The first one is on the human eval data set. So this is a code completion data set and all of the two clusters we identified. The second cluster, the agent is saying the LM response only includes the function definition and comments without any actual implementation or code to solve the problem. So this is an interesting finding, and when we look into the samples, we're showing the user queries here and the LM response, actually the fine tuned model is just repeating the initial user query without actually, without actually completing the code. So this shows that is where some instruction, following capability gap for this model, which models and with the generation approach to generate different variants of the instruction, the performance on this gap will be addressed do Yeah. And the second interesting cluster with 35 is on the kit QA data set. So this is a financial table based question answering data set. And the second cluster stood out to us, the agency saying the calculation of financial metrics related to percentage change. These are some common errors on that. And when we look into the samples, the question includes, what was the percentage change in acquisition and integration cost so the agent will be able to generate additional samples related to the percentage change to enforce the model's capability.

**Speaker 3 (39:06):**  
We also use the same experimentation to answer some design questions. The first question is, why we need all the agents in the workflow, and the second question is, how many samples do we generate as synthetic samples to answer the first question, we experimented with the full method as compared to different variants of the full method. By removing certain components, we tried removing the generalization pattern branch, the training error branch, as well removing the quality agent itself. For each of these variants, we see some performance degradation as compared to the full pipeline. So this indicates that the full pipeline achieves the maximum performance. And to answer the second question, how many samples we generate? We evaluated different numbers of generated sample in relation to the percentage of the training data. We tried out generating 50% of the original training data, 100% of the equal amount of the original training data, as well as 150% so initially, without more training data might always helps the model fine tuning. But it turns out not to be the case, and we find out that when we generate equal amounts of samples to the original training sample, performance has been maximized already.

**Speaker 3 (40:42):**  
So in the future work, we plan to expand to dynamic orchestrations with additional agents for fine tuning. In this solution, we have already developed hazard analysis, agent, data generation, agent, quality agent orchestrated by the overall orchestrator. But there could be room to develop other agents. For example, Agent focus focusing on data cleaning, how to do, how to resolve the original training data quality issue, or conduct additional processing of the original samples. Or it could be have a parameter. Agent which can suggest which hyper parameter to fine tune which values to utilize. And with all of this agent working together, we can design a dynamic orchestrator which will decide which agent to run, followed by which other agent.

**Speaker 3 (41:41):**  
So in conclusion, we have observed from the customers that there is a shift from using just large language models to while utilizing small language models to enjoy the benefits of lower cost as well as latency, to address some of the inherent capability gap for the small language model, we can leverage model fine tuning to improve its performance on specific tasks, any other, to improve the automation of iterative model fine tuning and data preparation. We design a multi agent data augmentation for small language fine tuning, any other to automate the fine tuning process to validate the augmentation strategy works or not, we have automated the entire fine tuning pipeline, from the initial configuration all the way to model evaluation and results. That

**Unknown Speaker (42:38):**  
concludes our talk. Thank you very much. Before

**Speaker 1 (42:46):**  
everybody believes. I wanted to also mention that this is actually based off of a research publication that our team worked on earlier this year, and that one actually this coming weekend, is going to be presenting at Yan. So we've included a link to the archive, the link to the paper on archive. First QR code that I mentioned before that we're part of the generative AI Innovation Center, and then the custom model program. So also some links here to learn more about Yan, see and also our specific program. It also has a little bit of a framework for how to think about model customization. Some of, like a recap of what we went over in the first part of the session, and then we took the liberty of writing some like, frequently asked questions we thought you guys might have. And so we'll kind of go over those and then open up the floor today from the audience. So first question we have is, how does this multi agent data authentication solution prevent data leakage if we're using the validation data set to improve

**Speaker 3 (43:44):**  
the training data, yeah, just before I provide answer to the question, please everyone before you leave to make sure complete quick survey on the app for this session. Yeah, yeah. So that's a good question. So in order to prevent the data leakage from the validation to the model training. Note that we only use the validation to generate some summarizations of what the model's weaknesses are and all the data augmentation they come from the training data itself. We just generate the variations of the training data with the guidance of the error patterns we identified from the validation set. So and also to note that when we gather the training data, this always opportunity to split the training data in the same way as cross validation to the actual training and the validation set, so there's no additional cost to collect other data as validation.

**Speaker 1 (44:43):**  
Awesome. So one other question, has the system itself been tested with multiple models for each agent, and what were your findings?

**Speaker 3 (44:53):**  
Yeah, so in the results, we have just showed the result of the fine tuning experiment for Yan model, but we have actually conducted experiments for the 3b model as well, although our finding was that the performance improvement for the slightly bigger, small language model was not as dramatic as the 1b model. And that's overall finding that the gap between the generation agent to the fine tuning smaller model is important. Previously we have also tried out to use using just the AP model as the agent, but it didn't work very well. I think one of the reason was that the model have to have sufficient instruction following capability to understand the task of hazard analysis and data generation. And second, the model needs to have adherence knowledge of generating additional samples that are not fully captured by the small language model for to improve its performance.

**Speaker 1 (45:57):**  
And then last question is on the automation. So how automated actually was the fine tuning process and how much manual human intervention was required after the automation?

**Speaker 3 (46:08):**  
Yeah, so the solution itself is actually fully automated. In our experiment, we have let it run overnight to generate multiple samples or multiple data sets. But I want to highlight that the solution is also inherently interpretable. Basically all of the patterns identify the generation strategy. They're just a few sentences in natural language. So as model developer, you can go into the artifact from the agent workflow and and, and you have additional knowledge about how to improve the generation. You can change those instructions for the generation. So this could be alternative for to improve performance further.

**Speaker 1 (46:56):**  
Awesome. Does anyone have any questions? Oh, great.

**Unknown Speaker (47:03):**  
So we're gonna hand Mike, so let's go first

**Speaker 4 (47:09):**  
two questions. One is you mentioned you're doing in the automated pipeline. You're doing the cluster based pattern analysis. Do you have any like human in the loop to review whether the cluster outcome is reasonable.

**Speaker 3 (47:28):**  
Yeah, so right now, the pipeline has been automated entirely. Has this no need to work, even in the loop to review and so on. But there's definitely a lot of human involvement,

**Unknown Speaker (47:41):**  
especially for prompt engineering agent,

**Unknown Speaker (47:47):**  
to make sure that all of the insights,

**Unknown Speaker (47:55):**  
so yeah, but after those have been.

